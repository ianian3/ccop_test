# 1차년도·2차년도 연구데이터 종합 검토 — 파이프라인 관점

> **작성일**: 2026-08-04
> **대상**: `/Users/iankwon/Downloads/1차년도`, `/Users/iankwon/Downloads/2차년도`
> **관점**: 하이브리드 3-트랙 파이프라인(문서→그래프)에 두 데이터셋이 어떻게 매핑되는가 — 즉시가능 vs 병목. **전 항목 실측 기반**.

---

## 1. 한눈 대조 (실측)

| 축 | 1차년도 | 2차년도 |
|---|---|---|
| 파일/용량 | 376 / 449MB | 431 / 297MB (log·pag·dir 221 제외 시 **210**) |

> ✏️ **2026-08-25 정정**: `.log` 81건은 멀티미디어가 아니라 **카카오톡 통신영장 회신 텍스트 로그**(EP6~8, 검색대상일/가입자 헤더)로 확인 — `vt_msg` 적재 트랙 B **즉시 가능** 대상(+81). `.pag`/`.dir` 140건만 WebDAV 캐시 부속(제외 유지). 상세: `docs/Y2_EP_DATASET_DIGEST_20260825.md` 부록.
| 최상위 | 00~06 (7개, 시나리오별) | 00 종합 · 01 유형별 (2개, 00에 집중) |
| **PDF** | **텍스트기반** ✅ (03중고나라 50: 영장·공소장·판결문) | **전부 스캔** ❌ (49, 표본10/10 0자 → OCR 필수) |
| **DOCX** | 5 | **40** (전화 가입자정보 카드, 반정형) |
| HWP | 56 (OLE, 파서필요) | 10 |
| 정형(xlsx/xls/csv) | 110 | 70 |
| 이미지/OCR대상 | 57 (png·jpg) | **78** (스캔pdf 49 + jpg 29) |
| **anb (i2 정답)** | **17** (05폴더 13 = 동심우 시나리오 정답) | 2 (우체국 역조회계좌) |
| 데이터 성격 | **사법 서사** (사건 재구성) | **통신 네트워크** (가입자·통화) |

---

## 2. 파이프라인 트랙 매핑

| 트랙 | 1차년도 | 2차년도 | 합계 |
|---|---|---|---|
| **A 정형** (ETL) | 110 | 70 | 180 |
| **B 비정형-즉시** (LLM, 파서OK) | pdf50 + hwpx3 + txt2 + docx5 ≈ **60** | **docx40** + txt3 ≈ **43** | ~103 |
| **B 비정형-hwp** (olefile 설치 필요) | 56 | 10 | 66 |
| **C 정답** (anb 대조평가) | 17 | 2 | 19 |
| **D 이미지** (OCR 필요) | 57 | **78** | 135 |

**비정형 총 224개** 중 **즉시 LLM 가능 ~103**, hwp파서 후 +66, OCR 후 +135.

---

## 3. 두 폴더 실증 결과 (파이프라인 동작 확인)

> ✏️ **2026-08-25 정정**: 아래 실증 수치를 저장본(`results/batch_doc_graph.json` · `results/batch_y2_docx.json`) 기준으로 정정. 초기 기재값(98/45, 276/59)은 별도 실행 시점 값으로 재현 불가하여 저장 산출물을 SoT로 삼음.

### 1차년도 — 03 중고나라 법률문서 6건
```
80 엔티티 / 32 관계 / 노드 10종·엣지 7종 / 할루시네이션 폐기 30타입·20값 (검증 통과 52)
🔗 크로스-문서 해소 3건: 동일 피의자 ← 4개 문서(영장·공소장·판결·조회)
```
→ **사건 재구성**: 흩어진 사법문서에서 피의자·계좌를 자동 통합.

### 2차년도 — 가입자정보 docx 8건
```
272 엔티티 / 56 관계 / 노드 4종 / 할루시네이션 폐기 3타입·4값 (검증 통과 267)
🔗 크로스-문서 해소 9건: 공유 IP 122.54.197.65 ← 3개 가입자 문서
```
→ **조직 SNA**: 같은 IP·연락처를 공유하는 복수 가입자를 자동 탐지(공범 단서). i2 Social Network Analysis를 자동화.

**결론**: 파이프라인이 성격이 다른 두 데이터(사법 서사 / 통신 네트워크)에서 모두 동작. 각각의 크로스-문서 해소가 서로 다른 수사가치(사건재구성 / 조직탐지)를 자동 산출.

---

## 4. 핵심 병목·리스크

1. **OCR이 최대 관문 (135개)** — 특히 **2차년도 수사서류(수사보고·범죄인지·영장)가 전부 스캔 PDF**. OCR 없이는 2차년도 *사건 본문*에 접근 불가 — docx(가입자정보)만으로는 통신망만 보이고 사건 서사가 비어있음. → **OCR 트랙(D)이 2차년도의 필수 선행조건**.
2. **hwp 66개** — 1차 미제사건 25 + 사이버마약 28 등 핵심 시나리오가 hwp. `olefile`(PrvText) 또는 `pyhwp` 필요.
3. **추출 품질(v46)** — 2차 docx에서 vt_telno 257 과다생성(통화상대 전부 노드화), 방향오류 잔존. 운영은 gpt-4o, 폐쇄망 v46 이원.
4. **정형 180개 미연결** — 트랙 A(ETL) 아직 미연결. 계좌거래·통화내역 원장이 여기 있어 그래프의 양적 본체.

---

## 5. 상호보완성 — 두 데이터를 합치는 가치

| | 1차년도 | 2차년도 | 통합 시 |
|---|---|---|---|
| 답하는 질문 | 누가 무엇을 했나(사건) | 누가 누구와 연결됐나(관계) | **완결적 수사그래프** |
| 그래프 | 사건-피의자-계좌 | 전화-IP-통화망 | 사건↔통신망 교차 |
| 크로스가치 | 사건 재구성 | 조직 탐지 | 사건별 조직도 |

두 anb 정답셋(19개)으로 각 트랙의 추출 정확도를 i2 대비 정량 검증 가능.

---

## 6. 권장 우선순위

1. **트랙 C — anb 정답 대조** (1차 05폴더 13개): CCOP 추출 vs i2 정답 P/R 정량화. "i2 대비 우위"의 객관 근거. *신규 파서만 필요, 데이터 확보됨.*
2. **트랙 D — OCR**: 2차년도 잠금 해제(수사서류 135개). 폐쇄망이면 온프레미스 OCR(Tesseract/PaddleOCR).
3. **트랙 B hwp 파서**: `olefile` 설치 → 66개 시나리오 해금.
4. **MERGE 실적재 + 트랙 A ETL**: AgensGraph 실그래프 완성.

---

## 부록 — 산출물
- 서비스: `app/services/document_extraction_service.py` (파서 어댑터 + 추출)
- 배치: `scripts/batch_doc_to_graph.py`
- 결과: `results/batch_doc_graph.json`(1차 법률), `results/batch_y2_docx.json`(2차 가입자)
- 설계: `docs/BATCH_DOC_TO_GRAPH_PIPELINE_20260804.md`
