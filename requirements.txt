# Web Framework
Flask==3.0.0
Werkzeug==3.0.1

# Database
psycopg2-binary==2.9.9

# Data Processing
pandas==2.1.4

# Environment Management
python-dotenv==1.0.0

# AI/ML
openai==2.15.0

# Agentic workflow (LangGraph) — required by app/services/langgraph_agent.py (routes.py import)
langgraph==0.6.11

# Production WSGI Server
gunicorn==21.2.0

# Security & CORS
flask-cors==4.0.0

# HTTP Requests (for health checks)
requests==2.31.0

# System Monitoring (선택) — monitoring_service.py 의 시스템 리소스 통계용
psutil==5.9.8

# (제거됨) Legal RAG(chromadb/pypdf/sentence-transformers): app 내 import 0건 — 죽은 의존성.
#   torch 등 수백 MB 절감. 재도입 시 별도 requirements 로 분리 권장.

# Testing
pytest==7.4.4
pytest-cov==4.1.0
