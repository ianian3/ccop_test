# Web Framework
Flask==3.0.0
Werkzeug==3.0.1

# Database
psycopg2-binary==2.9.9

# Data Processing
pandas==2.1.4

# Environment Management
python-dotenv==1.0.0

# AI/ML
openai==2.15.0

# Agentic workflow (LangGraph) — required by app/services/langgraph_agent.py (routes.py import)
langgraph==0.6.11

# Production WSGI Server
gunicorn==21.2.0

# Security & CORS
flask-cors==4.0.0

# HTTP Requests (for health checks)
requests==2.31.0

# System Monitoring (선택) — monitoring_service.py 의 시스템 리소스 통계용
psutil==5.9.8

# Legal RAG 선택 의존성은 base 에서 분리 — requirements-vector.txt 참조.
#   - pypdf: PDF 코퍼스 적재(개발 전용, scripts/ingest_legal_corpus.py 지연 import)
#   - chromadb: 벡터 백엔드=chroma 일 때만(LEGAL_VECTOR_BACKEND=chroma). 미설정 시 로드 0.
#   base(런타임)는 신규 의존성 0 유지 — 벡터 백엔드 기본값은 PostgreSQL BYTEA + numpy.

# Testing
pytest==7.4.4
pytest-cov==4.1.0

# 그래프 분석 엔진 (V4.8 — graph_analytics·graph_algo_service)
networkx==3.2.1
